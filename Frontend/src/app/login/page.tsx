import Example from "@/components/ui/login-form";

export const metadata = {
  title: "Sign In | PitchSide Pro Terminal",
  description: "Sign in to access the sports trading terminal workstation.",
};

export default function LoginPage() {
  return <Example />;
}
